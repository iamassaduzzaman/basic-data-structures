
#include <bits/stdc++.h>
using namespace std;

int main()
{
    list<int> l = {20, 40, 30, 10, 50, 10, 10};
    
    // l.remove(10);
    // l.sort();
    // l.sort(greater<int>());
    // l.unique();
    // l.reverse();
    
    
    for (int val: l)
    {
        cout << val << " ";
    }
    return 0;
}